<div class="container d-flex flex-wrap justify-content-between mb-5">
  <h2 class="d-inline"> Your Event</h2>
</div>

{{#with event}}
  {{!-- Tab  --}}
  <ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item" role="presentation">
      {{#> tabButton idName="description-tab" target="description"
        select="true"}}
        Description 
      {{/tabButton}}
    </li>
    <li class="nav-item" role="presentation">
      {{#> tabButton idName="category-tab" target="category"}}
        Categories 
      {{/tabButton}}
    </li>
    <li class="nav-item" role="presentation">
      {{#> tabButton idName="problem-tab" target="problem"}}
        Problems 
      {{/tabButton}}
    </li>
    <li class="nav-item" role="presentation">
      {{#> tabButton idName="assignment-tab" target="assignment"}}
        Problem Assignments
      {{/tabButton}}
    </li>
  </ul>

  {{!-- tab content --}}
  <div class="tab-content" id="myTabContent">
    
    {{!-- description --}}
    <div 
      class="tab-pane fade show active" 
      id="description" role="tabpanel" aria-labelledby="home-tab">
      {{> eventForm }}
    </div>

    {{!-- category --}}
    <div 
      class="tab-pane fade" 
      id="category" role="tabpanel" aria-labelledby="profile-tab">
      {{> categoryForm }}
    </div>
    
    {{!-- problem --}}
    <div 
      class="tab-pane fade" 
      id="problem" role="tabpanel" aria-labelledby="contact-tab">
      {{> problemForm }}
    </div>

    {{!-- problem assignment --}}
    <div 
      class="tab-pane fade" 
      id="assignment" role="tabpanel" aria-labelledby="assignment-tab">
      {{> assignmentForm }}
    </div>

  </div>

{{/with}}


<script src="/js/getEventId.js"></script>
<script src="/js/saveNewCategories.js"></script>
<script src="/js/saveNewProblems.js"></script>
<script src="/js/updateEvent.js"></script>