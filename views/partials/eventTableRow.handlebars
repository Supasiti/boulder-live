<tr 
  class="event-row"
  data-eventid="{{id}}">

  <th scope="row">{{id}}</th>
  <td>{{name}}</td>
  <td>{{location}}</td>
  <td>
    <span class="badge bg-success">{{status}}</span>
  </td>
  <td>
    {{#if canDelete }} 
      <button 
        class=" btn btn-sm btn-primary" 
        type="button">
        Cancel
      </button>
    {{/if }}
  </td>
</tr>